role_groups:
  bastion:
    name: Bastion roles

roles:
  internal.bastion.osLogin:
    groups: [bastion]
    name: 'role for access OSLogin API'
    visibility: internal
    description: 'role for access OSLogin API'
    permissions:
    - compute.osLogin.users.get

  bastion.user:
    groups: [bastion]
    name: 'role for Bastion user'
    visibility: internal
    description: ''
    permissions:
      - bastion.ssh.login
