oauth_clients:
  yc.oauth.datasphere:
    name: 'Datasphere WEB-application'
    includedRoles:
      - billing.accounts.viewer
      - datasphere.admin
      - dataproc.viewer
      - mdb.viewer
      - logging.reader
    scopes:
      - yc.resource-manager.folders.get
      - yc.resource-manager.clouds.get
      - yc.iam.serviceAccounts.get
