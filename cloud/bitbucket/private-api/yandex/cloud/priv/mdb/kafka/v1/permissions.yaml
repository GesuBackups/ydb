permissions:
  managed-kafka.topics.sync:
    description: Send updated topics info from Kafka cluster to Cloud API
    stage: GA
    visibility: internal
    resourceType: managed-kafka.cluster
    allowedWhen:
      cloud:
        status:
          - ACTIVE
