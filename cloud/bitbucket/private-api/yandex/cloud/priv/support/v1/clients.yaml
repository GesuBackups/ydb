oauth_clients:
  yc.oauth.support:
    name: 'Yandex Support WEB-application'
    includedRoles:
      - billing.clouds.owner
      - billing.accounts.admin
    scopes:
      - yc.resource-manager.folders.get
      - yc.resource-manager.clouds.get
      - yc.support.tickets.manage
