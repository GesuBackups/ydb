permissions:
  scheduler.instances.allocate:
    description: Allocate instance at node
    stage: GA
    visibility: internal
    allowedWhen:
      cloud:
        status:
          - ACTIVE
  scheduler.instances.dryRunAllocate:
    description: Check node's capacity using dry-run allocation
    stage: GA
    visibility: internal
    allowedWhen:
      cloud:
        status:
          - ACTIVE
  scheduler.instances.gcAllocations:
    description: Garbage collect allocated instances
    stage: GA
    visibility: internal
    allowedWhen:
      cloud:
        status:
          - ACTIVE
  scheduler.instances.getOutdated:
    description: List instances with outdated healthcheck
    stage: GA
    visibility: internal
    allowedWhen:
      cloud:
        status:
          - ACTIVE
  scheduler.nodes.getInfo:
    description: Get scheduled nodes info
    stage: GA
    visibility: internal
    allowedWhen:
      cloud:
        status:
          - ACTIVE
  scheduler.nodes.getInfoByName:
    description: Get scheduled nodes info by name
    stage: GA
    visibility: internal
    allowedWhen:
      cloud:
        status:
          - ACTIVE
  scheduler.nodes.delete:
    description: Delete node with all its allocations
    stage: GA
    visibility: internal
    allowedWhen:
      cloud:
        status:
          - ACTIVE
  scheduler.dedicatedHosts.replace:
    description: Replace assigned dedicated host
    stage: GA
    visibility: internal
    allowedWhen:
      cloud:
        status:
          - ACTIVE
  scheduler.dedicatedHosts.unsetStatus:
    description: Mark specific node as non-dedicated
    stage: GA
    visibility: internal
    allowedWhen:
      cloud:
        status:
          - ACTIVE
  scheduler.resources.get:
    description: List free resources on scheduled nodes
    stage: GA
    visibility: internal
    allowedWhen:
      cloud:
        status:
          - ACTIVE
  scheduler.hostGroups.resize:
    description: Resize host group of dedicated hosts
    stage: GA
    visibility: internal
    allowedWhen:
      cloud:
        status:
          - ACTIVE
  scheduler.dbSnapshots.save:
    description: Save state snapshot from DB
    stage: GA
    visibility: internal
    allowedWhen:
      cloud:
        status:
          - ACTIVE
  scheduler.info.get:
    description: Get scheduler info (name, status, initialized)
    stage: GA
    visibility: internal
    allowedWhen:
      cloud:
        status:
          - ACTIVE
  scheduler.daemon.restart:
    description: Restart the scheduler daemon
    stage: GA
    visibility: internal
    allowedWhen:
      cloud:
        status:
          - ACTIVE
