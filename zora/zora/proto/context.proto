package NZoraPb;

// User-specific context for passing through zora
// you can either use Data, or extend this message with a custom proto

message TContext {
    optional bytes Data = 1;

    extensions 100 to max;
}

