package NRobot;

option optimize_for = SPEED;

message TNavInfo {
    message TNavItem {
        optional string Query = 1;
        optional string AdHoc = 2;
    };

    repeated TNavItem NavItems = 1;
};
